import { StyleSheet } from 'react-native';

export const AppStyles = StyleSheet.create({
  
  buttonAgregar: {
    color:'white',
    backgroundColor: '#0093ff',
    borderRadius: 100,
    width: 60,
    height: 60,
    paddingBottom: 10,
    fontSize: 30,
  },

  buttonEliminar: {
    color: 'white',
    backgroundColor: 'red',
    fontSize: 16,
    fontWeight: 'bold',
  },
});